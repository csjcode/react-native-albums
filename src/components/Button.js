import React from 'react';
import { TouchableOpacity, Text } from 'react-native';

// Create a component

const Button = () => {
  return (
    <TouchableOpacity>
      <Text>Click me</Text>
    </TouchableOpacity>
  );
};

export default Button;
